apiVersion: apps/v1
kind: Deployment
metadata:
  name: cb-core
spec:
  selector:
    matchLabels:
      app: cb-core
  template:
    metadata:
      labels:
        app: cb-core
    spec:
      terminationGracePeriodSeconds: 120
      imagePullSecrets:
        - name: registrypullsecret
      containers:
      - name: cb-core
        image: harbor.baokim.vn/cb/cb-core:latest
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            memory: 256Mi
            cpu: 200m
          limits:
            memory: "1Gi"
        ports:
        - name: http
          containerPort: 80
        envFrom:
        - configMapRef:
            name: env-cb-core
        lifecycle:
          preStop:
            exec:
              command:
              - /bin/bash
              - -c
              - |
                echo "PreStop duoc kich hoat. Dang tam dung 60 giay de cho giao dich..."
                sleep 60


