apiVersion: apps/v1
kind: Deployment
metadata:
  name: cb-portal
spec:
  selector:
    matchLabels:
      app: cb-portal
  template:
    metadata:
      labels:
        app: cb-portal
    spec:
      terminationGracePeriodSeconds: 120
      imagePullSecrets:
        - name: registrypullsecret
      containers:
      - name: cb-portal
        image: harbor.baokim.vn/cb/cb-portal:v1.0.0
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            memory: 256Mi
            cpu: 200m
          limits:
            memory: "1Gi"
        ports:
        - name: http
          containerPort: 80
        envFrom:
        - configMapRef:
            name: env-cb-portal
        lifecycle:
          preStop:
            exec:
              command:
              - /bin/bash
              - -c
              - |
                echo "PreStop duoc kich hoat. Dang tam dung 60 giay de cho giao dich..."
                sleep 60
        volumeMounts:
          - mountPath: /var/www/html/storage/logs
            name: cb-portal-logs
      volumes:
        - name: cb-portal-logs
          persistentVolumeClaim:
            claimName: cb-logs
