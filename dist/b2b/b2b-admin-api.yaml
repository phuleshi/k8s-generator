apiVersion: apps/v1
kind: Deployment
metadata:
  name: b2b-admin-api
spec:
  selector:
    matchLabels:
      app: b2b-admin-api
  template:
    metadata:
      labels:
        app: b2b-admin-api
    spec:
      terminationGracePeriodSeconds: 120
      imagePullSecrets:
        - name: registrypullsecret
      containers:
      - name: b2b-admin-api
        image: harbor.baokim.vn/b2b/b2b-admin-api:latest
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            memory: 256Mi
            cpu: 200m
          limits:
            memory: "1Gi"
        ports:
        - name: http
          containerPort: 80
        envFrom:
        - configMapRef:
            name: env-b2b-admin-api
        lifecycle:
          preStop:
            exec:
              command:
              - /bin/bash
              - -c
              - |
                echo "PreStop duoc kich hoat. Dang tam dung 60 giay de cho giao dich..."
                sleep 60


