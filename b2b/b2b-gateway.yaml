apiVersion: apps/v1
kind: Deployment
metadata:
  name: b2b-gateway
spec:
  selector:
    matchLabels:
      app: b2b-gateway
  template:
    metadata:
      labels:
        app: b2b-gateway
    spec:
      terminationGracePeriodSeconds: 120
      imagePullSecrets:
        - name: registrypullsecret
      containers:
      - name: b2b-gateway
        image: harbor.baokim.vn/b2b/b2b-gateway:latest
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            memory: 256Mi
            cpu: 200m
          limits:
            memory: "1Gi"
        ports:
        - name: http
          containerPort: 80
        envFrom:
        - configMapRef:
            name: env-b2b-gateway
        lifecycle:
          preStop:
            exec:
              command:
              - /bin/bash
              - -c
              - |
                echo "PreStop duoc kich hoat. Dang tam dung 60 giay de cho giao dich..."
                sleep 60


